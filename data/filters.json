{
  "common": {
    "nom": { "type": "text" },
    "tiers": { "type": "enum" }
  },
  "moves": {
    "type": { "type": "enum" },
    "cible": { "type": "enum" },
    "portée": { "type": "enum" },
    "utilisation": { "type": "enum" },
    "concentration": { "type": "enum" },
    "évolutif": { "type": "enum" },
    "contact": { "type": "enum" },
    "cible": { "type": "enum" },
    "envergure": { "type": "enum" },
    "jet de la cible": { "type": "enum" }
  },
  "abilities": {
    "type": { "type": "enum" },
    "cible": { "type": "enum" }
  },
  "equipment": {
    "tiers": { "type": "enum" }
  },
  "items": {
    "tiers": { "type": "enum" }
  }
}